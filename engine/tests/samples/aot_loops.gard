function sum(n: int): int {
    let total = 0;
    let i = 0;
    while (i < n) {
        total = total + i;
        i = i + 1;
    }
    return total;
}

function main(): void {
    print(sum(100));
    print(sum(1000));
    print(sum(10000));
}
